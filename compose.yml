services:
  postgres:
    image: postgres:17-alpine
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: meeti
      POSTGRES_PASSWORD: meeti
      POSTGRES_DB: meeti
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: